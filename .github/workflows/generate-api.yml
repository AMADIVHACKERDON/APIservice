name: Generate API Boilerplate
on:
  push:
    paths:
      - 'openapi.yaml'
      - '.github/workflows/generate-api.yml'

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Generate Client SDK (Axios)
        uses: openapi-generators/openapitools-generator-action@v1
        with:
          generator: typescript-axios
          generator-tag: v7.0.0
          openapi-file: openapi.yaml
          command-args: >
            -o ./generated-api 
            --additional-properties=npmName=solutions-api-sdk,apiPackage=api,modelPackage=models,withSeparateModelsAndApi=true,useSingleRequestParameter=true,importFileExtension=.js

      - name: Generate Server Types (TypeScript)
        uses: openapi-generators/openapitools-generator-action@v1
        with:
          generator: typescript-node
          generator-tag: v7.0.0
          openapi-file: openapi.yaml
          command-args: >
            -o ./generated-server
            --additional-properties=supportsES6=true,modelPropertyNaming=camelCase

      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add ./generated-api ./generated-server
          git commit -m "Auto-update API types and SDK [skip ci]" || echo "No changes"
          git push
