import express from "express";

const app = express();
const PORT = 3000;

app.get("/v1/categories", () => {
    //
})

app.post("/", () => {
    //
})

app.listen(PORT, () => {console.log("Running....")})
